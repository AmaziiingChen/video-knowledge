# Copy to backend/.env for a local development checkout. Never commit backend/.env.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
PADDLE_OCR_ACCESS_TOKEN=
OBSIDIAN_VAULT=

# Keep experimental visual capture disabled in public builds.
MINIPROGRAM_FORUM_CAPTURE_ENABLED=false
