# KnowledgeHub Design Baseline

> 状态：2026-07-17 从当前 Vue/CSS 与本地运行页面提取。颜色、字体族、界面排版、圆角、控件尺寸、反馈色、焦点和动作 token 已部分接入 `frontend/src/styles/app.css`；间距与局部组件仍在迁移中。

## 产品视觉定位

KnowledgeHub 是 macOS 优先的高信息密度知识工作台。视觉核心不是“展示”，而是让用户长时间阅读、整理、分析内容时保持安静、清楚和可控。

- 四层表面：应用底色、中央工作区、面板、悬停层。
- 低对比表面与有限强调色只用于选择、进度、成功和关键动作；每个主题同时覆盖工作台与正文阅读，不允许用户手动拼配两套主题。
- 结构依赖细边界与表面明度差，阴影主要保留给浮层。
- 11–13px 是工具界面主体字号，20–30px 只用于页面或内容标题。
- 控件整体紧凑，常用高度为 28–40px。

### 界面方向

- 使用者：正在连续收集、阅读、转写和沉淀资料的研究者。
- 任务：快速判断内容状态，在资料、原文、字幕、AI 分析和处理日志之间保持上下文。
- 感受：像有批注痕迹的安静资料桌，而不是数据看板或营销后台。
- 深度：主结构只用表面明度和细边界；浮层才使用阴影。
- 形状：卡片、控件、菜单与浮层使用圆角矩形；中栏的资料预览窗格是永久工作台平面，Markdown/文档、图片、音频和视频的外边缘均为直角。音视频与转写时间轴进一步组成连续媒体工作区：预览、播放器/波形、分隔线和时间轴直角并无缝衔接。胶囊仅用于搜索、筛选和 chip，不可替代常规圆角矩形。
- 产品签名：“知识轨迹”橄榄色状态线，仅用于明确指定的主功能选中、标签页、字幕定位和处理进度；文件与资源行改用稳定的选中表面。

## 颜色

### 默认主题：清纸

| 语义 | Token | 值 | 用途 |
| --- | --- | --- | --- |
| 安静背景 | `--vk-bg-quiet` | `#FDF6E3` | 顶栏、状态栏、应用底色 |
| 中央背景 | `--vk-bg-center` | `#FCF8ED` | 编辑器和主要工作区 |
| 面板背景 | `--vk-bg-panel` | `#FFFDF7` | 侧栏、输入框、卡片 |
| 悬停背景 | `--vk-bg-hover` | `#F6EEDB` | hover、表头、轻选中 |
| 边界 | `--vk-border` | `#EADDCA` | 分栏、卡片、输入框 |
| 强调 | `--vk-accent` | `#93B259` | 主操作、进度、焦点 |
| 强强调 | `--vk-accent-strong` | `#6F8D3E` | 强调文字、成功状态 |
| 主文字 | `--vk-text` | `#2F2A1F` | 标题和正文 |
| 次文字 | `--vk-muted` | `#786D5A` | 辅助信息、占位符 |
| 动作背景 | `--vk-action-bg` | accent-strong 与 text 混合 | 主按钮、强选中项 |
| 动作前景 | `--vk-action-fg` | `#FFFDF7` | 动作背景上的文字和图标 |
| 轻选中背景 | `--vk-selected-bg` | accent 与 panel 混合 | 文件树、提示词等持续选中项 |
| 选中轨迹 | `--vk-selected-trace` | `--vk-accent-strong` | 仅用于规范明确指定的知识轨迹，不用于普通列表与文件树 |
| 拖拽标记 | `--vk-drag-indicator` / `--vk-drag-fill` | 随主题强调色派生 | 文件树拖拽位置和框选范围 |

公共层已经加入 `success`、`warning`、`danger`、`error-text`、对应的状态 surface，以及媒体、浮层和高亮 token；功能模块必须使用这些语义色。

Element Plus 的 primary、success、warning、danger、文字、表面、边界与 switch 色已在根节点桥接到 `--vk-*`。禁止在局部组件依赖 Element Plus 默认蓝色；仅在语义确有不同的控件上覆盖 `--el-*`。

### 默认主题对比度快照

| 组合 | 对比度 | 结论 |
| --- | --- | --- |
| `text` / `bg-quiet` | 13.23:1 | 充足 |
| `text` / `bg-panel` | 14.03:1 | 充足 |
| `muted` / `bg-quiet` | 4.71:1 | 已达到普通小字号文字常用的 4.5:1 基线 |
| `accent-strong` / `bg-panel` | 3.72:1 | 只适合较大或较粗文字，不宜承载 11–12px 关键信息 |
| `accent` / `bg-panel` | 2.36:1 | 应主要用于装饰、填充和状态提示，不宜直接作为小字号文字 |
| `action-fg` / `action-bg` | 5.32:1 | 可用于普通字号主按钮和强选中项 |

`accent` 继续承担品牌、进度和轻状态提示；主按钮与强选中项改用更深的 `action-bg`。这样把“品牌强调色”和“可读的动作底色”拆成了两个角色。

当前正式主题的 muted 小字和主动作均以 4.5:1 对比度为基线；暗色主题的正文与弱文本采用柔和深灰和高明度暖白，避免纯黑与纯白造成长时间阅读疲劳。

### 完整主题套件

| 主题 | Accent | Background | Foreground |
| --- | --- | --- | --- |
| 清纸 | `#6F8D3E` | `#FDF6E3` | `#2F2A1F` |
| 素白 | `#66756B` | `#FAFAF9` | `#252825` |
| Typora | `#428BCA` | `#FFFFFF` | `#333333` |
| 暖阅 | `#9E5D5A` | `#FAF4ED` | `#575279` |
| 羊皮纸 | `#CC7D5E` | `#F9F9F7` | `#2D2D2B` |
| 校稿 | `#3D755D` | `#F5F3ED` | `#2F312D` |
| 留白 | `#536B55` | `#F5F6F2` | `#28302B` |
| 简记 | `#3183D8` | `#FFFFFF` | `#37352F` |
| 晨雾 | `#0969DA` | `#FFFFFF` | `#1F2328` |
| 玫瑰松 | `#D7827E` | `#FAF4ED` | `#575279` |
| 雾紫 | `#8839EF` | `#EFF1F5` | `#4C4F69` |
| 蓝图 | `#0169CC` | `#FFFFFF` | `#0D0D0D` |
| 朱砂 | `#E64949` | `#FFFFFF` | `#030303` |
| 夜读 | `#9AB970` | `#1D211C` | `#E3E5D8` |
| 石墨 | `#B9C3B9` | `#1C1D1C` | `#E8EBE7` |
| 北境 | `#88C0D0` | `#2E3440` | `#ECEFF4` |

每套主题在 `frontend/src/styles/app.css` 中完整派生 quiet、center、panel、hover、border、accent、text、muted 与 reader token。暖阅、羊皮纸、玫瑰松使用宋体优先的 800px 版心；校稿和留白使用 700px 的深读版心；其余主题采用 820px 的系统无衬线版心。不得新增只换主色的“浅层主题”。

## 字体

### 字族

- 界面与正文：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`
- 品牌字标：`ui-monospace, "SFMono-Regular", "SF Mono", Menlo, Monaco, Consolas, monospace`，使用正常字形与中等字重，保持圆润、紧凑的 macOS 等宽观感
- 日志、路径、时间码：`ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace`

### 字号基线

| 层级 | 尺寸 | 典型用途 |
| --- | --- | --- |
| 极小 | 10px | 徽标、数量、紧凑元信息 |
| 辅助 | 11px | 表头、状态、时间、说明 |
| 工具正文 | 12px | 按钮、菜单、标签、侧栏 |
| 主界面正文 | 13px | 列表标题、输入、消息 |
| 阅读正文 | 14px | 文章、字幕、重要说明 |
| 小标题 | 15px | 面板标题、品牌文字 |
| 页面标题 | 20–22px | 设置页、管理页标题 |
| 内容标题 | 24–30px | 文章或视频详情标题 |

公共界面行高已收敛为 `1.2 / 1.3 / 1.4 / 1.55 / 1.65`；长文正文仍可使用 1.75。公共层现提供 `--vk-weight-regular / medium / strong / display` 四个角色，分别为 400、500、650、700；原有 450、540、560、580、620、640、720、760 已完成归并。

公共界面现已提供 `micro / meta / label / body / reading / heading / display` 七级排版 token，对应 10、11、12、13、14、15、21px。24–30px 的内容标题仍保留为阅读场景特例，不属于通用控件字号。

## 间距

公共层现提供语义化间距变量：`--vk-space-micro / xs / sm / control / cluster / panel / section / page / wide`，对应 2、4、6、8、12、16、24、32、38px。局部已有样式会按组件改造逐步接入，10、14、18、22px 继续作为经过确认的光学校正值。

`2 / 4 / 6 / 8 / 10 / 12 / 14 / 16 / 18 / 22 / 24 / 32 / 38px`

- 2–6px：图标、标签、紧凑列表内部。
- 8–12px：控件内部、同行控件间距。
- 14–18px：卡片和面板局部留白。
- 22–24px：页面级内容边距。
- 32–38px：宽工作区的响应式外边距。

这是一套从现有值整理出的尺度；新增或重构组件优先使用上述语义变量，已有组件不做无上下文的机械替换。

## 边界、圆角与阴影

### 边界

- 结构边界统一为 `1px solid var(--vk-border)`。
- 表格和次级分隔线通常把 border 混合到 57–84% 透明度。
- 焦点边界使用 accent；设置页额外增加 3px、18% accent 的外圈。
- 破坏性菜单项用顶部细线与普通操作分组。

### 建议收敛的圆角

| Token 角色 | 值 | 用途 |
| --- | --- | --- |
| structural | 12px | 历史别名；与 surface 一致，禁止再用于产生直角外轮廓 |
| compact | 4px | 小标签、字标、行内链接 |
| control | 8px | 图标按钮、菜单项 |
| input | 10px | 一般输入框、搜索结果头像 |
| surface | 12px | 卡片、设置输入框、对话框 |
| feature | 14px | 主要管理面板卡片 |
| pill | 999px | 搜索框、筛选器、chip、圆形控件 |

现有代码还大量使用 1、2、3、5、6、7、9、10、20px 和 50%，其中 6/7/9px 多是局部微调。实现阶段应逐组件确认，不能机械替换。

### 阴影

- 常规结构面：无外阴影，最多使用 `0 1px 0 ... inset` 提升边缘亮度。
- 浮层：约 `0 10px 26px`、12% text 混合色。
- 大型菜单/管理器：约 `0 16px 38px`、14% text 混合色。
- 消息提示：`0 14px 32px rgba(47, 42, 31, 0.14)` 并带轻微背景模糊。

### 强制形状规则

- 中栏资料预览窗格（`.workbench-editor-host`、`.tab-content-workspace`、`.content-media-frame`）统一使用 `border-radius: 0`；适用于 Markdown/文档、图片、音频和视频。音频或视频配合字幕/转写时间轴时，预览、内部播放器或波形、可拖动分隔线与时间轴属于同一连续媒体工作区，即使转写尚未载入也不得回退为圆角媒体卡片。此规则不适用于资料内部卡片、控件、菜单、弹窗或外层应用 chrome。
- 所有新建或改造的可见容器必须是圆角矩形，并使用 `--vk-radius-control`、`--vk-radius-input`、`--vk-radius-surface` 或 `--vk-radius-feature`；不得写入新的圆角数值。
- 所有弹窗、确认框、抽屉、popover、下拉菜单与上下文菜单一律使用至少 `--vk-radius-surface` 的圆角，并保持 `overflow: hidden`，**不允许直角边框、`border-radius: 0` 或 `border-radius: unset`**。
- 表格、列表和管理面板的外轮廓使用 `--vk-radius-surface`；表头与内容行必须共享同一列栅格，表头文字与对应内容列严格对齐。
- 内部行分隔线可以保持直线；圆角只定义外轮廓，不能通过为每一行堆叠独立卡片来替代。

## 控件尺寸

- 紧凑控件：28px，高频用于分页、chip、行内动作。
- 默认控件：约 34px，用于工具栏输入、选择器和按钮。
- 舒适控件：40px，主要用于设置页表单。
- 图标按钮：常见 28px 或 30px 方形点击区域。
- 顶栏 32px；Electron 桌面壳顶栏 40px；状态栏 22px。

## 控件状态

| 状态 | 当前视觉规则 | 交互规则 |
| --- | --- | --- |
| Default | panel 背景、text 文字、subtle border | 保持低视觉噪声 |
| Hover | `--vk-bg-hover` 或 5–12% accent tint | 只在支持 hover 的精确指针上增强 |
| Focus visible | accent 边界；表单可增加 18% accent halo | 不依赖 hover，必须可由键盘识别 |
| Active | 紧凑图标按钮可缩放至 0.97 | 按压反馈应短于 160ms |
| Selected | 7–10% accent tint 配合稳定文字色或字重 | 与 hover 必须有稳定差异；如无明确例外，不增加任一方向的强调线 |
| Disabled | muted 颜色，opacity 约 0.24–0.58 | cursor 使用 default 或 not-allowed；不能保留 hover |
| Loading | opacity 约 0.56、wait cursor 或 spinner | 阻止重复提交但保持布局稳定 |
| Success | accent-strong/绿色 | 用于完成、可用、连接正常 |
| Warning | 金棕色 | 用于未知、等待、可恢复异常 |
| Danger/Error | 红色文字或图标，少量淡红背景 | 破坏性动作与普通菜单项分组 |

### 已接入的公共状态

- Element Plus 主按钮使用 `action-bg/action-fg`，默认主题对比度为 5.32:1。
- 共享按钮按压反馈为 `scale(0.985)`，持续时间使用 120ms fast token。
- 全局按钮和链接具有键盘 `focus-visible` 轮廓。所有输入、文本域与选择器都保持静止：鼠标点击、输入与聚焦都不改变边框、阴影或位置，且禁止使用外层或 inset `box-shadow`；文本输入以插入光标、选择器以展开菜单和已选值表达当前状态。校验错误是唯一可改变输入表面的例外。
- 共享 disabled 基线为 0.48 opacity 和 `not-allowed` cursor；loading 保留 0.56 opacity。
- 文件与资源选中项使用轻选中背景和稳定文字色，不增加强调线；主功能、标签页、字幕轨和进度条是明确记录的橄榄色知识轨迹例外。
- 主页与提示词文件树共用 `SidebarTreeRow`：24px 行高、16px 层级缩进；文件夹只显示展开箭头，文件显示对应类型图标。重命名和删除操作仅在悬浮或键盘聚焦时立即显隐，不使用位移或淡入动画。
- 两套文件树的滚动槽都贴合主侧栏右边界并直接衔接拖动槽，内容右侧留白放在滚动容器内部；树行数量固定在距行右侧 8px 的同一列，悬浮操作在有操作的行中占用同一位置，不能推动数字横向漂移。
- 首页和提示词编辑器共用 `WorkspaceTabs`，不得为提示词页复制标签页结构或样式。标题溢出规则只作用于标题元素，未保存状态固定为 5×5px 语义圆点，不能继承标题宽度、遮罩或收缩规则。
- 提示词标签仅在用户可见草稿与打开时基线不同后进入未保存状态；任务切换、异步模板加载、启用状态刷新等程序写入不得触发未保存圆点或关闭确认。

## Web 界面规范审计

2026-07-16 已按当日最新的 Vercel Web Interface Guidelines 完成静态扫描与真实页面复核，并把稳定模式保存到 `.interface-design/system.md`。

- 页面语言、产品标题和主题色元信息已经明确；切换主题时同步更新浏览器主题色。
- 提供“跳到主内容”链接，主工作区可被程序化聚焦。
- 主功能导航使用 `aria-current`；工作区标签支持左右方向键、Home 和 End。
- 左右折叠栏、视频/字幕分隔线和处理日志分隔线均支持键盘展开或调整，并暴露当前值。
- 当前主界面、设置七个分区、公众号、缓存和提示词页面的可见表单均具有可访问名称；文本输入补充稳定 `name` 和合适的 `autocomplete`。
- 公众号二维码与头像具有固有尺寸；列表头像使用延迟加载和异步解码。
- “减少动态效果”下会移除循环 shimmer/pulse、加载旋转和空间位移动画，仅保留必要的短暂透明度反馈。
- Playwright 在 1440×900 和 820×760 下未发现页面级横向溢出、无名称按钮、缺失图片说明或控制台错误。

## 当前不一致之处

1. 圆角矩形已成为全局强制规则；仍需逐步迁移局部 0/6/7/9px 值，尤其是历史工作台结构和第三方组件覆盖。
2. shared disabled 已统一为 0.48，但局部组件仍保留 0.24、0.34、0.56、0.58 等覆盖。
3. 公共 focus contract 已建立，部分 scoped CSS 仍用自己的 outline 或把 hover 与 focus 合并。
4. 公共反馈色已 token 化；后续新增功能不得引入成功、警告、错误硬编码。
5. 间距、圆角、字号和阴影大多是字面量，新增页面容易继续漂移。
6. 个别功能模块仍有硬编码表面或状态色，夜读工作台下需继续逐页审计。
7. 16 套完整主题的状态色、边界和图标仍需在后续可访问性审计中逐页验证。

## 待确认的设计决策

以下方向已经开始实施，但仍有两项产品级取舍需要后续确认：

1. 16 套完整主题是否都长期维护；新增主题必须证明其资料整理或阅读场景与现有预设不同。
2. 是否为主题增加用户可调字号、行距和正文宽度；当前先提供经过校准的预设，避免设置面板变成排版工具。

已确定：所有可见界面表面使用圆角矩形；弹窗与全部浮层禁止直角边框。继续以 11–13px、28–34px 为主密度；长文阅读随所选完整主题使用相应的正文宽度、字体与行高。

## 产物与源文件

- 自动原始提取：`.extract-design-system/raw.json`
- 自动归一结果：`.extract-design-system/normalized.json`
- 人工校准 token：`design-system/tokens.json`
- CSS starter：`design-system/tokens.css`
- 当前颜色与主题源：`frontend/src/styles/app.css`
- 设置页控件覆盖：`frontend/src/styles/settings.css`
- 管理页模式：`frontend/src/styles/cacheManager.css`、`frontend/src/styles/wechatManager.css`
- 工作台交互状态：`frontend/src/workbench/*.vue`

自动提取器只识别出当前空白首屏的暖色背景、系统字体和少量距离，因此本文以代码审计为主、页面提取为辅。它不是完整组件库，也不应在未经逐页视觉验证时直接覆盖现有样式。
