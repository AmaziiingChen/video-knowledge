fastapi==0.139.0
uvicorn==0.51.0
pydantic-settings==2.14.2
httpx==0.28.1
requests==2.33.1
curl-cffi>=0.7.0
PyExecJS>=1.5.1
loguru>=0.7.3
retry>=0.9.2
faster-whisper==1.2.1
# MLX only runs on Apple Silicon; it is the preferred local runtime there.
mlx-whisper==0.4.3; sys_platform == "darwin" and platform_machine == "arm64"
openai==2.45.0
playwright==1.61.0
python-multipart==0.0.32
opencc-python-reimplemented==0.1.7
mcp==1.28.1
markdown-it-py==4.0.0
feedparser>=6.0
beautifulsoup4>=4.15.0
lxml==6.1.1
Pillow>=11.0
numpy>=1.26,<3
pyinstaller>=6.0,<7.0
# Loaded lazily by the campus event matcher. The report pipeline retains a
# deterministic lexical fallback when the local model has not been downloaded.
sentence-transformers>=4.1.0,<6.0.0
