# KnowledgeHub 开发协作约定

本文件适用于在本仓库中工作的开发者与编码代理。它约束开发过程，不改变产品运行逻辑。

## 目标优先

- 先明确本次要解决的用户问题、受影响链路与可验证的完成条件；不要把相邻的想法一并当作需求实施。
- 信息不足且会显著改变方案、数据或用户体验时，先说明缺口并请求确认；其余情况做最小、可逆的合理假设，并明确记录。
- 交付时报告实际改动、验证结果、已知边界；不能以“已开始”或理论可行代替完成。

## 先思考，再改代码

- 改动前先阅读相关实现、调用方、测试和已有文档，识别数据流、失败路径与性能边界。
- 涉及界面时，必须先阅读并遵守 [DESIGN.md](DESIGN.md)；复用现有组件、`--vk-*` token 和工作台结构，不另造一套视觉或交互语言。
- 涉及采集、下载、字幕或 AI 管线时，先确认现有阶段、去重、任务取消、重试和日志契约，避免绕开队列另建平行链路。
- 涉及 Cookie、登录态、令牌或个人内容时，默认本机保存、最小读取、日志脱敏；不得将明文凭据写入仓库、测试夹具或普通 SQLite 字段。

## 简单优先

- 优先选择能满足当前验收条件的最小设计：复用已有 API、组件、模型和依赖，而不是为一个局部问题引入框架、服务或抽象层。
- 不为推测中的未来需求预留复杂扩展点；确有跨模块重复或明确路线图时，才抽取公共能力。
- 启动链路、资料库首屏和后台调度以“先可用、后补齐”为原则：禁止把转写、浏览器自动化、OCR、批量正文/字幕读取等重工作塞进首屏阻塞路径。
- 对桌面端资源负责：默认单路下载；后台轮询低频、可取消；大数据列表分页和按需加载。

## 精准改动

- 只修改实现目标所必需的文件和代码路径；不做无关重命名、全仓格式化、依赖升级或顺手重构。
- 保持现有 API、持久化数据和用户配置的兼容性。需要迁移时，提供安全的迁移与回退语义。
- 对 bug 修复先定位根因；不要仅增加延迟、隐藏报错、吞掉异常或用前端占位掩盖后端状态错误。
- 不删除用户数据、缓存、凭据、任务或已有工作区状态，除非用户明确授权并且目标范围已经核实。

## 验证是完成的一部分

- 为行为变化补充或更新最贴近该行为的测试；不要以无关测试通过代替验证。
- 前端变更至少运行相关测试与 `npm run build`；后端变更至少运行相关 `pytest`。无法运行时，明确说明原因与未验证范围。
- 对性能或启动体验改动，报告可复现的测量或观察依据，而不是只描述预期收益。
- 完成后复查实际 diff，确认没有泄露凭据、没有无关改动，并检查空态、失败态、重试/取消及窄窗口等受影响边界。

## 现有设计与产品契约

- `DESIGN.md` 是视觉与交互基线；所有用户可见表面、弹窗、菜单和管理面板使用既有圆角 token，表头与内容共享同一列栅格，禁止引入直角外轮廓。
- `docs/ui-reference-and-workbench-spec.md` 定义工作台结构；资料、报告和工具页必须遵循“活动栏 / 主侧栏 / 编辑区 / 右侧上下文栏”的职责边界。
- `docs/product-development-plan.md` 定义产品范围；B 站遵循“可信字幕优先、下载与 ASR 兜底”，任务必须进入已有的持久化队列并能展示真实状态。
- 产品是本机优先的知识工作台。任何改动都应优先保护启动速度、可见反馈、数据准确性和用户对后台负载的可控性。
